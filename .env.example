# MedXP Project Root .env
# Used by: middleware, backend (via PYTHONPATH), malpractice_agent, start_all.py preflight
# Copy to .env:  cp .env.example .env

# Malpractice agent (middleware calls this)
MINIMAX_API_KEY=sk-cp-API_KEY_HERE

# Transcription via Gemini (middleware POST /api/transcribe)
GEMINI_API_KEY=KEY_HERE

# ------------------------------------------------------------------------------
# Other .env files required for full system:
# ------------------------------------------------------------------------------
# frontend/.env     - Vite/React env (VITE_*). Reference: frontend/.env.example
#                     Required for transcription to work: VITE_TRANSCRIBE_URL
# backend/.env      - Backend context enrichment. Reference: backend/.env.example
#                     Optional; backend has defaults.