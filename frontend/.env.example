# MedXP Frontend .env
# Used by: Vite (React). Vite reads .env from the frontend/ directory only.
# Copy to .env:  cp .env.example .env
# Restart the dev server after changing .env.

# Transcription API base URL. Points to middleware (Gemini) for PoC.
# Default if unset: http://localhost:8000 (backend-api, not used by start_all.py)
VITE_TRANSCRIBE_URL=http://localhost:5001